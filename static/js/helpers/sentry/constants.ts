// We should have only few sentry tags - they would help us search errors easily
// additional information should passed via extra / additional information
export const TAGS = {
  API: 'API',
  LOCALE: 'LOCALE',
};
